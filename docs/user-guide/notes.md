# Notes

* The terms "**move**" and "**movements**" are used interchangeably.
* The terms "**chained moves**" and "**sequence**" are used interchangeably.
* There already exists the functionality to allow much more advance movement (such as pieces phasing though each other) internally, however the UI for these functions are completed yet.
